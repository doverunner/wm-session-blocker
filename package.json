{
  "name": "lambda-wm-revoke-token",
  "version": "1.0.0",
  "description": "AWS Lambda function for revoking session using AWS WAF",
  "main": "index.js",
  "author": "server@doverunner.com",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "axios": "^1.11.0"
  },
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "@aws-sdk/client-wafv2": "^3.857.0",
    "jest": "^30.0.5"
  }
}
